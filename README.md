# HumanFactorUI

Dash 应用示例，复刻原型中的“人因信息展示 / 人因分析系统”界面，包含心率、情绪、空间分布等多种监测可视化。

## 运行

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

默认启动在 `http://0.0.0.0:8050/`。
